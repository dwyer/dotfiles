#!/bin/sh
tmp=`mktemp`
trap 'rm -f $tmp' 0
for file in "$@"
do
    if [ -e "$file" ]
    then
        expand -t 4 "$file" >$tmp
        cat $tmp >"$file"
    else
        echo $0: $file: no such file
    fi
done
